@model IEnumerable<Store_Sale_MovilPhone.Models.Marca>

@{
    ViewBag.Title = "Index";
}

<h2>Modelo de Celulares</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Nombre
        </th>
        <th>
            DetallesMarca
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DetallesMarca)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MarcaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MarcaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MarcaID })
        </td>
    </tr>
}


</table>
